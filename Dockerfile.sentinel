FROM python:3.12-slim

RUN apt-get update \
    && apt-get install --yes --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir .

EXPOSE 9010
ENTRYPOINT ["python", "-m", "entigram.sentinel_agent"]
